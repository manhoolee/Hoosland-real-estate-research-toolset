# 安全政策

## 报告漏洞

请不要在公开 Issue 中发布可利用细节、真实凭证、客户资料或未脱敏日志。优先使用 GitHub Security Advisories 私下报告，并提供：

- 受影响版本与组件；
- 最小复现步骤；
- 影响范围；
- 已采取的临时缓解措施；
- 不含真实秘密和客户内容的诊断证据。

## 敏感信息

仓库禁止包含：

- API key、密码、token、Cookie、私钥和连接串；
- `runtime-config.enc` 及其解密材料；
- 客户对话、附件、项目成果、操作日志和浏览器 profile；
- 真实服务器 IP、内部域名、无关 upstream 和生产拓扑；
- 未授权的第三方资料、图片、字体或数据库内容。

发现秘密已进入 Git 历史时，不要只删除工作区文件；应立即轮换凭证，并按组织流程清理历史和审计访问。

## 公开规则与运行时保密

版本化的 `backend/cordis.yml`、Skill 指令、Schema 和通用质量规则是公开源码，不属于秘密，也不能被当作安全边界。运行时仍不得通过业务接口回显动态组合后的提示词、隐藏上下文、工具凭证、内部工具结果或推理记录；真正的隔离依赖应用权限、沙箱、凭证最小化和输出过滤。

## 生产安全基线

- 使用独立非 root 服务用户和最小文件权限；
- 使用 HTTPS，并设置 `ADMIN_COOKIE_SECURE=true`；
- `/mcp` 只允许内部回连，公网必须阻断；
- 管理后台、普通用户入口和数据目录应有独立访问控制；
- 对业务磁盘、备份和日志使用组织级加密与保留策略；
- 两个槽位不共享可写 `DATA_DIR`、session 或 secret；
- 为模型、搜索、视觉、文档和生图 Provider 设置配额、费用和异常告警；
- 当前实现使用单 worker，不得通过多 worker 绕过进程内锁。

## 数据说明

Provider 密钥可加密保存，但对话、附件和成果本身是受操作系统权限保护的普通文件。管理员登录不等同完整多租户 IAM。部署者负责补充身份、授权、磁盘加密、备份、删除和合规策略。
